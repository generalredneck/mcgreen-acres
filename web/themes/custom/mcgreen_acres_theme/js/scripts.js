/**
 * @file
 * Behaviors for the mcgreen_acres_theme theme.
 */

(function ($, _, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.mcgreen_acres_theme = {
    attach: function (context) {

      // mcgreen_acres_theme JavaScript behaviors goes here.
    }
  };

})(window.jQuery, window._, window.Drupal, window.drupalSettings);
