services:
  cache.juicer_capture:
    class: Drupal\Core\Cache\DatabaseBackend
    arguments: ['juicer_capture', '@database', '@cache_tags.invalidator', '@serialization.phpserialize']

  juicer_capture.controller:
    class: Drupal\juicer_capture\Controller\JuicerCaptureController
    arguments: ['@cache.juicer_capture']
